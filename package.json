{
  "name": "university-email-deleter",
  "version": "1.0.0",
  "description": "A Node.js project to delete university emails.",
  "main": "server.js",
  "scripts": {
    "start": "npm server.js",
    "dev": "nodemon server.js",
    "build": "npm run clean && npm run build-js && npm run build-css && npm run copy-files",
    "clean": "rm -rf dist && mkdir dist",
    "build-js": "echo 'Compile JavaScript code here'",
    "build-css": "echo 'Minify CSS files here'",
    "copy-files": "cp -r public dist/public && cp favicon.ico dist/ && cp index.html dist/"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.18.2",
    "nodemailer": "^6.7.2",
    "serve-favicon": "^2.5.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
